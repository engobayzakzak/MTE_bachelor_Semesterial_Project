# Unique ID of the agent receiving the command
string agent_id

# Desired position (ENU frame)
geometry_msgs/Point target_position

# Desired orientation (quaternion)
geometry_msgs/Quaternion target_orientation

# Mode of the agent (e.g., search, return, dock, standby)
string mode
